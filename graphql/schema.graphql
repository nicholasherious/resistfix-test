### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Comment {
  authorId: String
  id: String
  post: Post
  postId: String
  text: String
  user: User
}

type Like {
  like: Post!
  user: User!
}

type Mutation {
  createComment(authorId: String, postId: String, text: String!): Comment
  createPost(authorId: String, mediaPublicId: String, mediaUrl: String, resourceType: String, title: String!): Post
  deletePost(id: String!): Post
  editPost(category: String, description: String, id: String!, imageUrl: String, title: String!, url: String): Post
  like(likeId: String!): Like
  replyToComment(authorId: String, parentCommentId: String, postId: String, text: String!): Comment
}

type Post {
  authorId: String
  category: String
  comments: [Comment]
  id: String
  mediaPublicId: String
  mediaUrl: String
  resourceType: String
  title: String
  user: User
}

type Query {
  comments: [Comment]!
  posts: [Post]!
  singleComment(postId: String!): Comment
  singlePost(postId: String!): Post
  users: [User]!
}

enum Role {
  ADMIN
  USER
}

type User {
  comments: [Comment]
  email: String
  id: String
  image: String
  name: String
  posts: [Post]
  role: Role
  username: String
}